# 專案規則（Claude Code 自動載入）

這是 Danny Lin 個人作品集網站的 repo。每次修改前，請務必遵守以下規則：

1. **先讀取 `DESIGN.md`**，依照其中的設計規格執行，並保留所有已確認的設計決策。DESIGN.md 是本專案的唯一設計規格來源，包含色彩系統、字體、各區塊規格、RWD 規則，以及明確的 ❌ 禁止事項（例如：不用翻轉卡、不用手風琴、案例封面不用實際照片、工具名稱不加 emoji）。

2. **修改完成後，同步更新 `DESIGN.md` 的「七、維護紀錄」**，新增一筆格式為 `| YYYY/MM/DD | 調整了什麼 |` 的紀錄。這是唯一的異動歷史記錄，不另建 CHANGELOG。

3. **圖片路徑為相對路徑**（`images/檔名`），新增圖片時檔名需與 `GALLERIES` 物件或 `<img src>` 中引用的完全一致（含大小寫、副檔名）。

4. **單一檔案架構**：全站僅 `index.html` 一個程式碼檔案，CSS 與 JS 皆內嵌，不拆分成多檔案，除非使用者明確要求重構。

5. 若異動涉及設計系統本身（配色、字體、版式規則），先在 `DESIGN.md` 對應章節更新規格說明，再落地到 `index.html`，避免規格文件與實際程式碼不同步。
